//Ternary operator i.e..the operators that work in 3 ooperands

let marks = 30;

let result = marks >= 40 ? "pass" : "fail";
console.log(result);
//ternary operators are more compact compared to if- else statements
